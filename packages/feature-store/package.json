{
  "name": "feature-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "pnpm exec tsc -p tsconfig.json --watch",
    "build": "pnpm exec tsc -p tsconfig.json",
    "typecheck": "pnpm exec tsc -p tsconfig.json",
    "lint": "pnpm exec eslint src tests --max-warnings=0",
    "test": "pnpm exec vitest run --config vitest.config.ts --coverage"
  },
  "dependencies": {
    "zod": "3.24.2"
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "3.0.5",
    "pg-mem": "3.0.12"
  }
}
